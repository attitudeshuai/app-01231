<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.module.system.mapper.SystemConfigMapper">

    <resultMap id="BaseResultMap" type="com.library.module.system.entity.SystemConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="description" property="description"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, config_key, config_value, description, updated_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config ORDER BY config_key ASC
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config
        <where>
            <if test="params.configKey != null and params.configKey != ''">
                AND config_key LIKE CONCAT('%', #{params.configKey}, '%')
            </if>
        </where>
        ORDER BY config_key ASC
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config
        <where>
            <if test="params.configKey != null and params.configKey != ''">
                AND config_key LIKE CONCAT('%', #{params.configKey}, '%')
            </if>
        </where>
        ORDER BY config_key ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM system_config
        <where>
            <if test="params.configKey != null and params.configKey != ''">
                AND config_key LIKE CONCAT('%', #{params.configKey}, '%')
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO system_config (config_key, config_value, description, updated_at)
        VALUES (#{configKey}, #{configValue}, #{description}, NOW())
    </insert>

    <insert id="batchInsert">
        INSERT INTO system_config (config_key, config_value, description, updated_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.configKey}, #{item.configValue}, #{item.description}, NOW())
        </foreach>
    </insert>

    <update id="updateById">
        UPDATE system_config
        <set>
            <if test="configKey != null">config_key = #{configKey},</if>
            <if test="configValue != null">config_value = #{configValue},</if>
            <if test="description != null">description = #{description},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM system_config WHERE id = #{id}
    </delete>

    <delete id="deleteBatchIds">
        DELETE FROM system_config WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM system_config
        <where>
            <if test="params.id != null">AND id = #{params.id}</if>
            <if test="params.configKey != null">AND config_key = #{params.configKey}</if>
        </where>
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config WHERE config_key = #{configKey}
    </select>

    <select id="selectByKeyPrefix" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM system_config 
        WHERE config_key LIKE CONCAT(#{prefix}, '%')
        ORDER BY config_key ASC
    </select>

    <update id="updateValue">
        UPDATE system_config 
        SET config_value = #{configValue}, updated_at = NOW() 
        WHERE config_key = #{configKey}
    </update>

    <update id="batchUpdateValues">
        <foreach collection="list" item="item" separator=";">
            UPDATE system_config 
            SET config_value = #{item.configValue}, updated_at = NOW() 
            WHERE config_key = #{item.configKey}
        </foreach>
    </update>
</mapper>
