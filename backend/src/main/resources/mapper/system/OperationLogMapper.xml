<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.module.system.mapper.OperationLogMapper">

    <resultMap id="BaseResultMap" type="com.library.module.system.entity.OperationLogEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="ip" property="ip"/>
        <result column="time_cost" property="timeCost"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, username, operation, method, params, ip, time_cost, status, error_msg, created_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM operation_logs WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM operation_logs ORDER BY created_at DESC
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM operation_logs
        <where>
            <if test="params.username != null and params.username != ''">
                AND username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.operation != null and params.operation != ''">
                AND operation LIKE CONCAT('%', #{params.operation}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM operation_logs
        <where>
            <if test="params.username != null and params.username != ''">
                AND username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.operation != null and params.operation != ''">
                AND operation LIKE CONCAT('%', #{params.operation}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.startDate != null">
                AND created_at >= #{params.startDate}
            </if>
            <if test="params.endDate != null">
                AND created_at &lt;= #{params.endDate}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectPageLogs" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM operation_logs
        <where>
            <if test="params.username != null and params.username != ''">
                AND username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.operation != null and params.operation != ''">
                AND operation LIKE CONCAT('%', #{params.operation}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.startDate != null">
                AND created_at >= #{params.startDate}
            </if>
            <if test="params.endDate != null">
                AND created_at &lt;= #{params.endDate}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM operation_logs
        <where>
            <if test="params.username != null and params.username != ''">
                AND username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.operation != null and params.operation != ''">
                AND operation LIKE CONCAT('%', #{params.operation}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.startDate != null">
                AND created_at >= #{params.startDate}
            </if>
            <if test="params.endDate != null">
                AND created_at &lt;= #{params.endDate}
            </if>
        </where>
    </select>

    <select id="countLogs" resultType="long">
        SELECT COUNT(*) FROM operation_logs
        <where>
            <if test="params.username != null and params.username != ''">
                AND username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.operation != null and params.operation != ''">
                AND operation LIKE CONCAT('%', #{params.operation}, '%')
            </if>
            <if test="params.status != null">
                AND status = #{params.status}
            </if>
            <if test="params.startDate != null">
                AND created_at >= #{params.startDate}
            </if>
            <if test="params.endDate != null">
                AND created_at &lt;= #{params.endDate}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_logs (user_id, username, operation, method, params, 
                                    ip, time_cost, status, error_msg, created_at)
        VALUES (#{userId}, #{username}, #{operation}, #{method}, #{params}, 
                #{ip}, #{timeCost}, #{status}, #{errorMsg}, NOW())
    </insert>

    <insert id="batchInsert">
        INSERT INTO operation_logs (user_id, username, operation, method, params, 
                                    ip, time_cost, status, error_msg, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.username}, #{item.operation}, #{item.method}, 
             #{item.params}, #{item.ip}, #{item.timeCost}, #{item.status}, #{item.errorMsg}, NOW())
        </foreach>
    </insert>

    <update id="updateById">
        UPDATE operation_logs
        <set>
            <if test="operation != null">operation = #{operation},</if>
            <if test="status != null">status = #{status},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM operation_logs WHERE id = #{id}
    </delete>

    <delete id="deleteBatchIds">
        DELETE FROM operation_logs WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM operation_logs
        <where>
            <if test="params.id != null">AND id = #{params.id}</if>
        </where>
    </select>

    <delete id="deleteBeforeDate">
        DELETE FROM operation_logs WHERE created_at &lt; #{beforeDate}
    </delete>
</mapper>
