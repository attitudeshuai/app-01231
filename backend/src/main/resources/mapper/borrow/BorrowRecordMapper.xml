<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.module.borrow.mapper.BorrowRecordMapper">

    <resultMap id="BaseResultMap" type="com.library.module.borrow.entity.BorrowRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="book_id" property="bookId"/>
        <result column="borrow_date" property="borrowDate"/>
        <result column="due_date" property="dueDate"/>
        <result column="return_date" property="returnDate"/>
        <result column="renew_count" property="renewCount"/>
        <result column="fine_amount" property="fineAmount"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createdAt"/>
        <result column="username" property="username"/>
        <result column="book_title" property="bookTitle"/>
        <result column="isbn" property="isbn"/>
        <result column="author" property="author"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, book_id, borrow_date, due_date, return_date, 
        renew_count, fine_amount, status, remark, created_at
    </sql>

    <sql id="Join_User_Book">
        FROM borrow_records br
        LEFT JOIN users u ON br.user_id = u.id
        LEFT JOIN books b ON br.book_id = b.id
    </sql>

    <sql id="Detail_Where_Clause">
        <where>
            <if test="params.userId != null">
                AND br.user_id = #{params.userId}
            </if>
            <if test="params.status != null">
                AND br.status = #{params.status}
            </if>
            <if test="params.username != null and params.username != ''">
                AND u.username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.bookTitle != null and params.bookTitle != ''">
                AND b.title LIKE CONCAT('%', #{params.bookTitle}, '%')
            </if>
            <if test="params.startDate != null">
                AND br.borrow_date >= #{params.startDate}
            </if>
            <if test="params.endDate != null">
                AND br.borrow_date &lt;= #{params.endDate}
            </if>
        </where>
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        ORDER BY br.created_at DESC
    </select>

    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        <where>
            <if test="params.userId != null">
                AND br.user_id = #{params.userId}
            </if>
            <if test="params.bookId != null">
                AND br.book_id = #{params.bookId}
            </if>
            <if test="params.status != null">
                AND br.status = #{params.status}
            </if>
        </where>
        ORDER BY br.created_at DESC
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        <where>
            <if test="params.userId != null">
                AND br.user_id = #{params.userId}
            </if>
            <if test="params.bookId != null">
                AND br.book_id = #{params.bookId}
            </if>
            <if test="params.status != null">
                AND br.status = #{params.status}
            </if>
            <if test="params.username != null and params.username != ''">
                AND u.username LIKE CONCAT('%', #{params.username}, '%')
            </if>
            <if test="params.bookTitle != null and params.bookTitle != ''">
                AND b.title LIKE CONCAT('%', #{params.bookTitle}, '%')
            </if>
        </where>
        ORDER BY br.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectPageWithDetails" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        <include refid="Detail_Where_Clause"/>
        ORDER BY br.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countWithDetails" resultType="long">
        SELECT COUNT(*)
        <include refid="Join_User_Book"/>
        <include refid="Detail_Where_Clause"/>
    </select>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM borrow_records br
        <where>
            <if test="params.userId != null">
                AND br.user_id = #{params.userId}
            </if>
            <if test="params.status != null">
                AND br.status = #{params.status}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO borrow_records (user_id, book_id, borrow_date, due_date, 
                                    renew_count, fine_amount, status, remark, created_at)
        VALUES (#{userId}, #{bookId}, #{borrowDate}, #{dueDate}, 
                #{renewCount}, #{fineAmount}, #{status}, #{remark}, NOW())
    </insert>

    <insert id="batchInsert">
        INSERT INTO borrow_records (user_id, book_id, borrow_date, due_date, 
                                    renew_count, fine_amount, status, remark, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.bookId}, #{item.borrowDate}, #{item.dueDate},
             #{item.renewCount}, #{item.fineAmount}, #{item.status}, #{item.remark}, NOW())
        </foreach>
    </insert>

    <update id="updateById">
        UPDATE borrow_records
        <set>
            <if test="dueDate != null">due_date = #{dueDate},</if>
            <if test="returnDate != null">return_date = #{returnDate},</if>
            <if test="renewCount != null">renew_count = #{renewCount},</if>
            <if test="fineAmount != null">fine_amount = #{fineAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM borrow_records WHERE id = #{id}
    </delete>

    <delete id="deleteBatchIds">
        DELETE FROM borrow_records WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 FROM borrow_records
        <where>
            <if test="params.id != null">AND id = #{params.id}</if>
        </where>
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.user_id = #{userId}
        ORDER BY br.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM borrow_records WHERE user_id = #{userId}
    </select>

    <select id="selectBorrowingByUserId" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.user_id = #{userId} AND br.status IN (0, 2)
        ORDER BY br.due_date ASC
    </select>

    <select id="selectBorrowingByUserIdAndBookId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM borrow_records
        WHERE user_id = #{userId} AND book_id = #{bookId} AND status IN (0, 2)
        LIMIT 1
    </select>

    <select id="countBorrowingByUserId" resultType="int">
        SELECT COUNT(*) FROM borrow_records 
        WHERE user_id = #{userId} AND status IN (0, 2)
    </select>

    <select id="selectOverdue" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.status IN (0, 2) AND br.due_date &lt; #{now}
        ORDER BY br.due_date ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countOverdue" resultType="long">
        SELECT COUNT(*) FROM borrow_records 
        WHERE status IN (0, 2) AND due_date &lt; #{now}
    </select>

    <select id="selectDueSoon" resultMap="BaseResultMap">
        SELECT br.*, u.username, u.email, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.status = 0 
          AND br.due_date >= #{startDate} 
          AND br.due_date &lt;= #{endDate}
        ORDER BY br.due_date ASC
    </select>

    <update id="updateReturn">
        UPDATE borrow_records 
        SET return_date = #{returnDate}, 
            fine_amount = #{fineAmount}, 
            status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateRenew">
        UPDATE borrow_records 
        SET due_date = #{dueDate}, 
            renew_count = #{renewCount}
        WHERE id = #{id}
    </update>

    <update id="updateOverdueStatus">
        UPDATE borrow_records 
        SET status = 2 
        WHERE status = 0 AND due_date &lt; #{now}
    </update>

    <select id="selectStatistics" resultType="map">
        SELECT 
            COUNT(*) as total_borrows,
            SUM(CASE WHEN status = 1 THEN 1 ELSE 0 END) as returned_count,
            SUM(CASE WHEN status IN (0, 2) THEN 1 ELSE 0 END) as borrowing_count,
            SUM(CASE WHEN status = 2 THEN 1 ELSE 0 END) as overdue_count,
            COALESCE(SUM(fine_amount), 0) as total_fine
        FROM borrow_records
        WHERE created_at >= #{startDate} AND created_at &lt;= #{endDate}
    </select>

    <!-- 查询即将到期的借阅记录（用于邮件提醒） -->
    <select id="selectDueSoonRecords" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.status = 0 
          AND DATE(br.due_date) >= #{today}
          AND DATE(br.due_date) &lt;= #{reminderDate}
        ORDER BY br.due_date ASC
    </select>

    <!-- 查询逾期的借阅记录（用于邮件通知） -->
    <select id="selectOverdueRecords" resultMap="BaseResultMap">
        SELECT br.*, u.username, b.title as book_title, b.isbn, b.author
        <include refid="Join_User_Book"/>
        WHERE br.status IN (0, 2) 
          AND DATE(br.due_date) &lt; #{today}
        ORDER BY br.due_date ASC
    </select>
</mapper>
